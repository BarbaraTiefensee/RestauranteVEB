
@{
    ViewData["Title"] = "Cadastrar";
}

<br />
<h1>Cadastro de Refeição</h1>
<br />

<form method="post">
    <div class="form-group">
        <label for="Nome">Nome</label>
        <div class="form-label-group">
            <input type="text" id="Nome" name="Nome" class="form-control">
            <span id="erroNome" name="erroNome" class="text-danger"></span><br />
        </div>
    </div>

    <div class="form-group">
        <label for="Preco">Preco</label>
        <div class="form-label-group">
            <input type="text" id="Preco" name="Preco" class="form-control">
            <span id="erroPreco" name="erroPreco" class="text-danger"></span><br />
        </div>
    </div>

    <div class="row">
        @if (ViewBag.MensagemErro != null)
        {
            <div class="form-group">
                <div class="col-md-10">
                    <span class="text-danger">@ViewBag.MensagemErro</span>
                </div>
            </div>
        }

        <div class="form-group">
            <div class="col-md-offset-2">
                <input type="submit" value="Cadastrar" class="btn btn-primary btn-block" id="BotaoEnviar" name="BotaoEnviar" />
                <input type="button" value="Limpar" class="btn btn-primary btn-block" id="BotaoLimpar" name="BotaoLimpar" />
            </div>
        </div>
    </div>
</form>

@section scripts{

    <script type="text/javascript">

        $(document).ready(function () {

            $("#BotaoEnviar").click(function (e) {

                var sucesso = true;
                var Nome = $("#Nome").val();
                var Preco = $("#Preco").val();

                if (Nome == "") {
                    $("#erroNome").html("O nome deve ser informado.");
                    sucesso = false;
                }
                else {
                    if (Nome.length < 5 || Nome.length > 50) {
                        $("#erroNome").html("O nome deve conter no mínimo 5 a 50 caracteres.");
                        sucesso = false;
                    }
                    else {
                        $("#erroNome").html("");
                    }
                }

                if (Preco == "") {
                    $("#erroPreco").html("O Preco deve ser informado.");
                    sucesso = false;
                }
                else {
                    $("#erroPreco").html("");
                }

                if (!sucesso) {
                    e.preventDefault();
                }
            });

            $("#BotaoLimpar").click(function () {
                $("#Nome").val("");
                $("#Preco").val("");
            });

        });

    </script>
}

